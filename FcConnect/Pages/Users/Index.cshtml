@page
@model FcConnect.Pages.Users.IndexModel

@{
    ViewData["Title"] = "User Management";
    Guid clickCheck = Guid.NewGuid();
    HttpContext.Session.SetString("UserEditClick", clickCheck.ToString());
}

<h4>User Management</h4>

<p>
    <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary"><i class="bi bi-person-add"></i>&nbsp;Add new user</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User[0].Forename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User[0].Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User[0].Email)
            </th>
            <th>
                User Role
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.User) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Forename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @{
                    string role;
                    int roleId = item.RoleId;
                    switch (roleId)
                    {
                        case Constants.RoleUser:
                            role = "User";
                            break;
                        case Constants.RoleAdmin:
                            role = "Admin";
                            break;
                        case Constants.RoleDeveloper:
                            role = "Developer";
                            break;
                        default:
                            role = "Unknown";
                            break;
                    }
                }
                @role

            </td>
            <td>
                <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@item.Id" asp-route-click="@clickCheck"><i class="bi bi-pencil"></i>&nbsp;Edit</a> 
                <a class="btn btn-danger" asp-page="./Delete" asp-route-id="@item.Id" asp-route-click="@clickCheck"><i class="bi bi-trash3"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
